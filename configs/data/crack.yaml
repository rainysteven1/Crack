_target_: src.data.CrackDataModule
data_dir: ${paths.data_dir}/crack500

class_weights: [0.06647518381057083, 0.9999999983956792]

dataset:
  _target_: src.data.components.CrackDataset
  _partial_: true
  img_size: 256
  is_boundary: false

train_batch_size: 8 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
test_batch_size: 16
num_workers: 0
pin_memory: true
persistent_workers: false
