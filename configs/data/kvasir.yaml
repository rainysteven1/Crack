_target_: src.data.kvasir_datamodule.KvasirDataModule
train_data_dir: ${paths.data_dir}/Kvasir-SEG-aug
test_data_dir: ${paths.data_dir}/CVC-ClinicDB
train_batch_size: 16 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
test_batch_size: 16
num_workers: 0
pin_memory: true
persistent_workers: false
