# @package _global_

defaults:
  - _self_
  - data: crack # choose datamodule with `test_dataloader()` for evaluation
  - model: double_unet
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
